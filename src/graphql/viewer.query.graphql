query viewer {
    viewer {
        id
        name
        avatarUrl
        createdAt
        starredRepositories {
            totalCount
        }
        status {
            id
            message
        }
    }
}